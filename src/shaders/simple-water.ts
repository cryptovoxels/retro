import { SceneContext, ShaderMaterial } from '@babylonjs/lite'

export class SimpleWater {
  constructor(_scene: SceneContext) {
    // todo(lite): simpleocean shader material
  }

  getMaterial(): ShaderMaterial {
    return null as any
  }

  dispose(): void {}
}
