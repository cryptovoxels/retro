# ABOUTME: Example env for local renderer runs.

DATABASE_URL=postgres://localhost/voxels
UGC_ACCESS=
UGC_SECRET=
PORT=8080
# Main API for /api/* proxy (islands.json etc when hitting local orbit pages).
API_ORIGIN=http://127.0.0.1:9000
